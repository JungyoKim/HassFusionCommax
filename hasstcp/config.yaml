# hasstcp 설정 파일

# SSH 원격 캡처 설정
ssh:
  host: "192.168.0.60:22"
  user: "root"
  password: "1!@Honami"
  # key: "C:\\Users\\사용자\\.ssh\\id_rsa"  # 키 파일 사용시 (password 대신)
  command: "tcpdump -nn -i eth0.2 -U -s 0 -w - not port 22"

# MQTT 설정 (Home Assistant 연동)
mqtt:
  broker: "tcp://192.168.0.15:1883"
  client_id: "testttest"  # 비워두면 자동으로 랜덤 ID 생성
  username: ""
  password: ""
  topic: "hasstcp/parking"

# 네트워크 설정
network:
  # 내부망 CIDR (인바운드/아웃바운드 판정용, 생략 가능)
  # local_cidrs:
  #   - "10.0.0.0/8"
  #   - "192.168.0.0/16"

